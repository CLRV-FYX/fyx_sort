# BENCHMARKS — 本机实测

> 所有数字都在本机**实测**跑出，没有编造。复现方式见文末。

## 环境

- CPU：Intel Xeon Ice Lake-SP（2 个硬件线程 / vCPU）
- 编译器：g++ 12.2.0，`-O3 -march=native -pthread`
- 堆：glibc，默认分配器
- 方法：`fyx::sort` 与 `std::sort` 各排序同一份随机数据的副本，取多次运行中的**最好**一次。
  吞吐 = 输入字节数 / 时间。加速比 = `std_time / fyx_time`。

## 结果（单线程）

| type   | size       | fyx (s)   | std (s)   | fyx GB/s  | std GB/s  | speedup |
|--------|------------|-----------|-----------|-----------|-----------|---------|
| int32  |     4.2 MB |    0.012 s |    0.074 s |   0.36 GB/s |   0.06 GB/s |  6.29x |
| int32  |    67.1 MB |    0.209 s |    1.417 s |   0.32 GB/s |   0.05 GB/s |  6.77x |
| int32  |   536.9 MB |    1.737 s |   12.822 s |   0.31 GB/s |   0.04 GB/s |  7.38x |
| uint32 |    67.1 MB |    0.205 s |    1.419 s |   0.33 GB/s |   0.05 GB/s |  6.93x |
| int64  |   134.2 MB |    0.411 s |    1.416 s |   0.33 GB/s |   0.09 GB/s |  3.45x |
| uint64 |   134.2 MB |    0.402 s |    1.452 s |   0.33 GB/s |   0.09 GB/s |  3.62x |
| float  |    67.1 MB |    0.190 s |    1.499 s |   0.35 GB/s |   0.04 GB/s |  7.88x |
| double |   134.2 MB |    0.351 s |    1.521 s |   0.38 GB/s |   0.09 GB/s |  4.33x |

## 怎么读这些数据

- **数值类型明显快于 `std::sort`**：int32/float ~6–8×，int64/double ~3–4×。
  这来自 LSD 基数排序（数值 + 默认 `<` 比较器时自动启用），而不是更聪明的比较排序。
- **单线程吞吐约 0.3–0.4 GB/s**。这**远低于**「单线程 8 GB/s」之类的营销口号——
  那在物理上不可能（详见 `DESIGN.md` 第 0 节的带宽论证）。本机单核可用内存带宽本就
  只有 ~8–20 GB/s 量级，基数排序还要读 + 写 + 直方图，实测 0.3 GB/s 是合理且诚实的数字。
- **64 位类型比 32 位慢**：基数排序的趟数翻倍（8 趟 vs 4 趟），且单位字节的键更宽，
  符合预期。
- **`stable_sort` 数值升序走基数（天然稳定），不牺牲速度**；非数值 / 降序走归并排序。

## 没有测、也没法在本沙箱验证的东西

- **多核并行 ≥30 GB/s**：本机只有 2 个 vCPU，并行分治在这里测不出多核带宽。
  `Options.parallel == On` 的代码路径**已编译并通过正确性测试**（见 `test/t_api.cpp`
  的 `Tri::On` 用例），但带宽数字需要多核机器才能给出。
- **GPU 路径**：本沙箱无 GPU，GPU 层是骨架 + CPU 回退，未实测。
- **与 ips4o / vqsort / x86-simd-sort 的横向对比**：沙箱无外网，下载不到这些库，未做。
  本库的目标是「在同一数量级内竞争，并在若干场景取胜」，不是宣称通吃。

## 复现

```bash
./build.sh
g++ -std=c++17 -O3 -march=native -pthread bench/bench_api.cpp -o /tmp/bench_api
/tmp/bench_api
```
