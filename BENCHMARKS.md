# BENCHMARKS — 本机实测

> 下面每一个数字都是在这台机器上跑出来的，包含输的格子。复现命令在文末。

## 环境

- CPU：Intel Xeon Ice Lake-SP，**2 个硬件线程**（L1d 48 KiB / L2 1.3 MiB / L3 54 MiB，AVX-512 全套）
- 编译器：g++ 12.2.0，`-O3 -march=native -pthread`
- 方法：每个格子先复制一份输入，只对 `fyx::sort` 计时（复制不计），取多次运行的最好一次。
- 对手：`std::sort`（libstdc++）、`pdqsort`（本库自带）、**`vqsort`（Google Highway 1.4.0）**。
  加速比按「并行 `fyx::sort` vs 三者中最好的那个」计算——也就是说，1.00x 是打平，小于 1 是输。
- `string` 行没有 vqsort 数字：vqsort 只处理算术类型，对 `std::string` 区间什么都不做，
  所以那一行退化为与 `std::sort` / `pdqsort` 比。

## 一、随机数据（最难的一类）

| 类型 | 规模 | 串行 | 并行 | std::sort | vqsort | 并行 vs std | 并行 vs 最强对手 |
|------|------|------|------|-----------|--------|-------------|------------------|
| int32 | 100 万 | 0.00749 s | 0.00459 s | 0.07826 s | 0.00371 s | 17.1x | 0.81x (vqsort) |
| int32 | 800 万 | 0.09828 s | 0.04943 s | 0.76846 s | 0.03919 s | 15.5x | 0.79x (vqsort) |
| int64 | 100 万 | 0.00912 s | 0.00552 s | 0.08057 s | 0.00771 s | 14.6x | 1.40x (vqsort) |
| int64 | 800 万 | 0.11960 s | 0.06703 s | 0.78706 s | 0.08547 s | 11.7x | 1.28x (vqsort) |
| double | 100 万 | 0.01189 s | 0.00632 s | 0.08556 s | 0.00683 s | 13.5x | 1.08x (vqsort) |
| double | 800 万 | 0.13534 s | 0.08242 s | 0.84194 s | 0.08159 s | 10.2x | 0.99x (vqsort) |

## 二、结构化数据（真实数据里最常见的形状）

| 类型 | 分布 | 规模 | 并行 | std::sort | 并行 vs std | 并行 vs 最强对手 |
|------|------|------|------|-----------|-------------|------------------|
| int32 | 已排序 | 800 万 | 0.00617 s | 0.13116 s | 21.3x | 2.02x (pdqsort) |
| int32 | 逆序 | 800 万 | 0.00498 s | 0.10515 s | 21.1x | 4.03x (pdqsort) |
| int32 | 近似有序 | 800 万 | 0.00792 s | 0.14135 s | 17.9x | 5.60x (vqsort) |
| int32 | 16 个不同值 | 800 万 | 0.00847 s | 0.23344 s | 27.6x | 1.22x (vqsort) |
| int32 | 全部相等 | 800 万 | 0.00273 s | 0.09012 s | 33.0x | 0.77x (vqsort) |
| int32 | 周期为 8 | 800 万 | 0.01074 s | 0.13478 s | 12.5x | 0.83x (vqsort) |
| int64 | 已排序 | 800 万 | 0.01093 s | 0.12627 s | 11.6x | 1.80x (pdqsort) |
| int64 | 逆序 | 800 万 | 0.00857 s | 0.09817 s | 11.5x | 3.45x (pdqsort) |
| int64 | 近似有序 | 800 万 | 0.01177 s | 0.13070 s | 11.1x | 7.32x (pdqsort) |
| int64 | 16 个不同值 | 800 万 | 0.01929 s | 0.24537 s | 12.7x | 1.14x (vqsort) |
| int64 | 全部相等 | 800 万 | 0.00564 s | 0.13007 s | 23.1x | 0.81x (vqsort) |
| int64 | 周期为 8 | 800 万 | 0.01559 s | 0.16546 s | 10.6x | 1.24x (vqsort) |
| double | 已排序 | 800 万 | 0.01249 s | 0.13469 s | 10.8x | 1.71x (pdqsort) |
| double | 逆序 | 800 万 | 0.01056 s | 0.09840 s | 9.3x | 3.14x (pdqsort) |
| double | 近似有序 | 800 万 | 0.01492 s | 0.14097 s | 9.4x | 6.14x (vqsort) |
| double | 16 个不同值 | 800 万 | 0.01875 s | 0.27525 s | 14.7x | 1.38x (vqsort) |
| double | 全部相等 | 800 万 | 0.00590 s | 0.13669 s | 23.2x | 1.88x (vqsort) |
| double | 周期为 8 | 800 万 | 0.02088 s | 0.18771 s | 9.0x | 1.13x (vqsort) |

## 三、全矩阵战绩

矩阵覆盖 4 种类型 × 14 种分布 × 2 个规模，每个格子与三者中最好的那个比：

- **100 万：胜 35 / 负 7**（共 42 个算术格子）
- **800 万：胜 37 / 负 5**（共 42 个算术格子）

### 输的格子（诚实清单）

| 规模 | 类型 | 分布 | 本库并行 | 对手 | 比值 |
|------|------|------|----------|------|------|
| 100 万 | double | 周期为 8 | 0.00144 s | vqsort 0.00137 s | 0.95x |
| 100 万 | int32 | 远距离交换 | 0.00449 s | vqsort 0.00395 s | 0.88x |
| 100 万 | int32 | 16 个不同值 | 0.00090 s | vqsort 0.00069 s | 0.76x |
| 100 万 | int32 | 周期为 8 | 0.00101 s | vqsort 0.00050 s | 0.50x |
| 100 万 | int32 | 随机 | 0.00459 s | vqsort 0.00371 s | 0.81x |
| 100 万 | int64 | 16 个不同值 | 0.00265 s | vqsort 0.00133 s | 0.50x |
| 100 万 | int64 | 周期为 8 | 0.00176 s | vqsort 0.00114 s | 0.65x |
| 800 万 | double | 随机 | 0.08242 s | vqsort 0.08159 s | 0.99x |
| 800 万 | int32 | 全部相等 | 0.00273 s | vqsort 0.00210 s | 0.77x |
| 800 万 | int32 | 周期为 8 | 0.01074 s | vqsort 0.00887 s | 0.83x |
| 800 万 | int32 | 随机 | 0.04943 s | vqsort 0.03919 s | 0.79x |
| 800 万 | int64 | 全部相等 | 0.00564 s | vqsort 0.00456 s | 0.81x |

## 四、怎么读这些数字

- **随机数据是唯一系统性落后的地方**，其余分布几乎全线领先。随机均匀键是所有基于分布/分治
  排序的最坏情况：它没有任何结构可利用，只能老老实实搬字节。
- **并行加速受这台机器的内存带宽限制**：2 个硬件线程跑不出多核带宽，流式的活（扫描、memcpy）
  两线程约等于一线程；只有计算密集的核（直方图、散射）才吃得到第二个核。
- **结构化数据的领先来自「先识别结构」**：已排序/逆序/全等用一次扫描证明，近似有序和拼接
  走自然游程归并，低基数走计数排序。这些都是 O(n) 而不是 O(n log n)。
- **输的格子绝对值都很小**（多数在 0.5–10 ms），但它们是真实的差距，不是噪声：
  低基数的瓶颈在计数趟，随机 int32 的瓶颈在散射趟的随机写。

## 五、没有测、也不能在本机验证的

- **4 核以上的并行带宽**：本机只有 2 个硬件线程，多核扩展性只能靠代码结构保证，数字测不出来。
- **GPU 路径**：无 GPU，代码是骨架 + CPU 回退，未实测。
- **其它第三方库**（ips4o、x86-simd-sort）：只对比了 Highway 的 vqsort。

## 复现

```bash
./build.sh
bash tools/dev/vqsort.sh 1000000     # 或 8000000；克隆 Highway 1.4.0 并跑全矩阵
python3 tools/fyx_test.py             # 743 项正确性检查
for f in test/t_*.cpp; do g++ -std=c++17 -O2 -march=native -pthread -I. $f -o /tmp/t && /tmp/t; done
```

